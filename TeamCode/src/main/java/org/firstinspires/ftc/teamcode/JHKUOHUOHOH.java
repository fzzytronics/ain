package org.firstinspires.ftc.teamcode;

import com.arcrobotics.ftclib.kinematics.Odometry;
import com.arcrobotics.ftclib.geometry.Pose2d;
import com.arcrobotics.ftclib.kinematics.Odometry;
import org.firstinspires.ftc.robotcore.external.Telemetry;
public class JHKUOHUOHOH<telemetry> {
    ///BRUHBRUH
    //MEWMEW
    public void Pose2d(double x,
                       double y) {
        double getX;
        double getY;
        double getHeading;
    }

    private Pose2d getPoseMeters() {
        getPoseMeters().getX();
        getPoseMeters().getY();
        getPoseMeters().getHeading();
        return null;
    }

    private void telemetry {
        telemetry.addData ("getX"), Pose2d.getX();
        telemetry.addData ("getY"), Pose2d.getY();
        telemetry.addData ("getAngle"), Pose2d.getHeading();
}
}